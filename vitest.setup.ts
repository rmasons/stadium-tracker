// Registers @testing-library/jest-dom matchers (e.g. toBeInTheDocument) with
// Vitest's expect, and augments the types project-wide. Loaded via
// vitest.config.ts's setupFiles.
import "@testing-library/jest-dom/vitest";
